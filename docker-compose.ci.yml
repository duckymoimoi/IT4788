# ==============================================================
# HOSPITAL NAVIGATION SYSTEM - Docker Compose
# 2 containers: Go API + PostgreSQL
# ==============================================================
# Chay:  docker compose up --build
# Dung:  docker compose down
# Reset: docker compose down -v  (xoa data PostgreSQL)
# ==============================================================

services:
  # ---- PostgreSQL Database ----
  db:
    image: postgres:17-alpine
    container_name: hospital-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: hospital
      POSTGRES_USER: admin_hospital
      POSTGRES_PASSWORD: $2a$1
    # ports:
    #   - "5433:5432"  # REMOVED TO PREVENT BOTNET ATTACKS
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin_hospital -d hospital"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - proxy-net

  # ---- Go API Server ----
  group3-api:
    image: ghcr.io/duckymoimoi/it4788:latest
    container_name: hospital-api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      APP_ENV: production
      PORT: "8080"
      DB_DSN: "host=db user=admin_hospital password=$$2a$$12$$SecureRandomPassword2026! dbname=hospital port=5432 sslmode=disable"
      JWT_SECRET: "hospital-prod-secret-change-me-2026"
    volumes:
      - uploads:/app/uploads       # Giu file uploads giua cac lan restart
      - audio:/app/audio           # Cache TTS audio files
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.group3-api.rule=Host(`group3.it4788.sukkaito.id.vn`)"
      - "traefik.http.routers.group3-api.entrypoints=web"
      - "traefik.http.services.group3-api.loadbalancer.server.port=8080"
    networks:
      - proxy-net

volumes:
  pgdata:                          # PostgreSQL data persist
  uploads:                         # User uploaded files
  audio:                           # TTS voice files cache
networks:
  proxy-net:
    external: true
