import { Typography } from 'antd';

const { Title, Text } = Typography;

export default function MapEditor() {
  return (
    <>
      <Title level={4}>Map Editor</Title>
      <Text type="secondary">👤 B — Quản lý bản đồ grid + CRUD nodes</Text>
    </>
  );
}
