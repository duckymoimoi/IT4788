import { Typography } from 'antd';

const { Title, Text } = Typography;

export default function ChatSupport() {
  return (
    <>
      <Title level={4}>Chat Support</Title>
      <Text type="secondary">👤 E — Chat hỗ trợ, WebSocket realtime</Text>
    </>
  );
}
